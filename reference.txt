loadScript(scriptName)

setting(settingName)

loadMap(bgName)

loadTileset(tilesetName)

loadFont(fontFilename) <--include file extension for image

playMusic(musicFilename)

stopMusic()

wait(frames)

show()
hide()

move(mobName,destX,destY,speed,entryPath) //entryPath can be entervert or enterhor

add(mobName,x,y)
add(mobName,x,y,characterFile)

textbox(speaker,dialogue,side)
textbox(speaker,dialogue,side,font)

messagebox(message)

titlescreen(filename)

fight(leftFighter,rightFighter,attacker,damage)
kill(mobName)

setCamera(x,y)
panCamera(x,y,speed,entryPath)

skyboxColor(red,green,blue,alpha)